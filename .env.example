FLEET_NODES=500
FLEET_GPUS_PER_NODE=8
SIM_DAYS=30
SIM_SEED=42
RENEWABLE_MIX=1.0
ELECTRICITY_PRICE_USD_PER_KWH=0.06

# Optional — if set, the weekly report uses Claude to write the narrative section.
# Get a key from https://console.anthropic.com/. Leave blank to use the
# deterministic template fallback.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
